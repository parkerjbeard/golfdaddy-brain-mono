# Render.com Frontend Environment Variables
# Copy these to your Render frontend service environment settings
# Service name: brain-frontend (or your frontend service name)

# API Configuration - CRITICAL FOR AUTHENTICATION
# Must point to your Render backend service URL
VITE_API_BASE_URL=https://brain-api.onrender.com
VITE_API_URL=https://brain-api.onrender.com

# Supabase Configuration (Client-side Auth)
VITE_SUPABASE_URL=https://your_project_id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Environment
VITE_ENV=production

# Optional: Feature Flags
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_DEBUG_MODE=false
VITE_ENABLE_DEV_TOOLS=false

# Optional: External Services
VITE_SENTRY_DSN=https://your_sentry_dsn_here@sentry.io/project_id
VITE_GA_TRACKING_ID=G-XXXXXXXXXX

# Build Configuration
NODE_ENV=production

# Optional: API Keys for Frontend Services
# Only include public keys here - never include secret keys in frontend
VITE_STRIPE_PUBLIC_KEY=pk_live_your_stripe_public_key
VITE_MAPBOX_PUBLIC_TOKEN=pk.your_mapbox_public_token

# Optional: App Configuration
VITE_APP_NAME=GolfDaddy Brain
VITE_APP_VERSION=1.0.0
VITE_MAX_FILE_SIZE=10485760  # 10MB in bytes
VITE_SESSION_TIMEOUT=3600000  # 1 hour in milliseconds

# IMPORTANT NOTES:
# 1. Backend URL: Must match your deployed backend service URL exactly
# 2. Never include secret keys or sensitive data in frontend env vars
# 3. All VITE_ prefixed variables are exposed to the browser
# 4. Supabase anon key is safe to expose (it's meant for client-side)
# 5. After adding these, click "Save Changes" and wait for redeploy
# 6. The frontend will NOT work without correct VITE_API_BASE_URL

# TROUBLESHOOTING:
# If authentication isn't working:
# 1. Verify VITE_API_BASE_URL points to your backend (not frontend!)
# 2. Ensure both services are deployed and running
# 3. Test backend directly: https://brain-api.onrender.com/docs
